# Archetype Next.js

## Commands

```shell
# Install dependencies
pnpm install

# Check code style and linting (via mise task)
pnpm run check

# Fix code style and linting issues (via mise task)
pnpm run fix
```
